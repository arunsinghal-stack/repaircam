# RepairCam recorder dependencies.  Needs Python 3.8 or newer
# (Ubuntu 20.04 ships 3.8, which is what the shop recorder runs).
# Install with:  python3 -m pip install -r requirements.txt
#
# Everything else (recording, concat, snapshots) is done by the ffmpeg binary,
# which is installed with apt, not pip:  sudo apt install ffmpeg

PyYAML>=6.0          # reads repaircam/cameras.yaml
Flask>=3.0           # Phase 2 web UI
